import * as three from "https://esm.sh/three@0.158.0";
import * as act from '../../core/mod.ts';
import * as recon from '../../recon/mod.ts';

export const createThreeRenderer = () => {
  
  return { recieveDeltas };
}
